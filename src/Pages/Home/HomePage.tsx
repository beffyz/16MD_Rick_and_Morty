import { useNavigate } from 'react-router-dom';

const HomePage = () => {
  const navigate = useNavigate();

  return (
    <div>
      <span>
        Home Page
      </span>
    </div>
  );
};

export default HomePage;
